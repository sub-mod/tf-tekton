---
apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: tf-build-pipeline
spec:
  resources:
    - name: git-repo
      type: git
    - name: build-image
      type: image
  tasks:
    - name: tf-build-run
      taskRef:
        name: buildah
      params:
        - name: DOCKERFILE
          value: Dockerfile.centos6
        - name: TLSVERIFY
          value: "false"
      resources:
        inputs:
          - name: source
            resource: git-repo
        outputs:
          - name: image
            resource: build-image
    - name: tf-task-run
      taskRef:
        name: tf-run
      resources:
        outputs:
          - name: source
            resource: git-repo
      runAfter:
        - tf-build-run
